<div class="container-fluid min-vh-100 d-flex flex-column">
  <div class="row flex-grow-1">
    <!-- Sidebar -->
    <nav class="col-md-3 col-lg-2 bg-dark text-white p-3">
      <h4 class="text-center">Painel Admin</h4>
      <div class="d-flex justify-content-center">
        <small class="text-secondary">Gerencie os usuários</small>
      </div>
      <hr>
      <ul class="nav flex-column">
        <li class="nav-item"><a href="/admin/painel" class="nav-link text-white">📊 Visão Geral</a></li>
        <li class="nav-item"><a href="/admin/usuarios" class="nav-link text-white">👥 Gerenciar Usuários</a></li>
        <li class="nav-item"><a href="/admin/eventos" class="nav-link text-white">📅 Gerenciar Eventos</a></li>
        <li class="nav-item"><a href="/admin/categorias" class="nav-link text-white">📑 Categorias</a></li>
      </ul>
    </nav>

    <!-- Conteúdo -->
    <main class="col-md-9 col-lg-10 p-4">
      <h2 class="mb-4">Gerenciamento de Usuários</h2>

      <table class="table table-striped table-bordered align-middle">
        <thead class="table-dark">
          <tr>
            <th>Nome</th>
            <th>Email</th>
            <th>Admin</th>
            <th>Saldo</th>
            <th>Ações</th>
          </tr>
        </thead>
        <tbody>
          {{#each usuarios}}
            <tr>
              <td>{{this.nome}}</td>
              <td>{{this.email}}</td>
              <td>{{#if this.eAdmin}}Sim{{else}}Não{{/if}}</td>
              <td>R$ {{this.saldo}}</td>
              <td>
                <a href="/admin/usuarios/edit/{{this._id}}" class="btn btn-sm btn-primary">Editar</a>
                <form action="/admin/usuarios/delete/{{this._id}}" method="POST" class="d-inline">
                  <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Tem certeza que deseja excluir este usuário?')">Excluir</button>
                </form>
              </td>
            </tr>
          {{/each}}
        </tbody>
      </table>
    </main>
  </div>
</div>