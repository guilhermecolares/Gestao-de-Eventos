<div class="container-fluid min-vh-100 d-flex flex-column">
  <div class="row flex-grow-1">
    <!-- Sidebar -->
    <nav class="col-md-3 col-lg-2 bg-dark text-white p-3">
      <h4 class="text-center">Painel Admin</h4>
      <div class="d-flex justify-content-center">
        <small class="text-secondary">Gerencie os eventos</small>
      </div>
      <hr>
      <ul class="nav flex-column">
        <li class="nav-item"><a href="/admin/painel" class="nav-link text-white">📊 Visão Geral</a></li>
        <li class="nav-item"><a href="/admin/usuarios" class="nav-link text-white">👥 Gerenciar Usuários</a></li>
        <li class="nav-item"><a href="/admin/eventos" class="nav-link text-white">📅 Gerenciar Eventos</a></li>
        <li class="nav-item"><a href="/admin/categorias" class="nav-link text-white">📑 Categorias</a></li>
      </ul>
    </nav>

    <!-- Conteúdo -->
    <main class="col-md-9 col-lg-10 p-4">
      <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>Gerenciamento de Eventos</h2>
        <a href="/admin/eventos/add" class="btn btn-success">+ Novo Evento</a>
      </div>

      {{#if eventos.length}}
        <table class="table table-striped table-bordered align-middle">
          <thead class="table-dark">
            <tr>
              <th>Título</th>
              <th>Categoria</th>
              <th>Data</th>
              <th>Preço</th>
              <th>Ações</th>
            </tr>
          </thead>
          <tbody>
            {{#each eventos}}
              <tr>
                <td>{{this.titulo}}</td>
                <td>{{this.categoria.nome}}</td>
                <td>{{formatarData this.data}}</td>
                <td>{{#if this.gratuito}}Grátis{{else}}R$ {{this.preco}}{{/if}}</td>
                <td>
                  <a href="/admin/eventos/edit/{{this._id}}" class="btn btn-sm btn-primary">Editar</a>
                  <form action="/admin/eventos/delete/{{this._id}}" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Deseja realmente excluir este evento?')">Excluir</button>
                  </form>
                </td>
              </tr>
            {{/each}}
          </tbody>
        </table>
      {{else}}
        <div class="alert alert-warning">Nenhum evento encontrado.</div>
      {{/if}}
    </main>
  </div>
</div>