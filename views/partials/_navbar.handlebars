<nav class="navbar navbar-expand-lg bg-dark navbar-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/index">NJS Events</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/index">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-danger" href="/usuarios/logout">Logout</a>
        </li>
      </ul>

      {{#if exibirSaldo}}
<!-- Saldo dentro da navbar -->
<div class="d-flex align-items-center text-white">
  <span class="me-2">💰 Saldo: R$ <span id="saldo">{{saldo}}</span></span>
  <button id="toggleSaldoForm" class="btn btn-sm btn-success">+</button>
</div>
{{/if}}
    </div>
  </div>
</nav>

<!-- Caixa para adicionar saldo (escondida por padrão) -->
{{#if exibirSaldo}}
<div id="saldoForm" class="bg-dark text-white p-3 rounded shadow-sm position-absolute end-0 mt-2 me-4" style="display: none; width: 220px; z-index: 1050;">
  <form id="formAdicionarSaldo">
    <label for="valorSaldo">Adicionar Saldo:</label>
    <input type="number" id="valorSaldo" name="valor" class="form-control form-control-sm mt-1" placeholder="Digite o valor" min="1" max="10000">
    <button type="submit" class="btn btn-primary btn-sm mt-2 w-100">Confirmar</button>
  </form>
</div>
{{/if}}