<div class="d-flex">
    <!-- Sidebar -->
    <nav class="bg-dark text-white p-3 min-vh-100 flex-shrink-0" style="width: 250px;">
        <h4 class="text-center">Menu</h4>
        <div class="d-flex justify-content-center">
                <small class="text-secondary">Eventos Inscritos</small>
            </div>
        <hr>
        <ul class="nav flex-column">
            <li class="nav-item">
                <a href="/eventos/" class="nav-link text-white">✅ Criar Evento</a>
            </li>
            <li class="nav-item">
                <a href="/eventos/meuseventos" class="nav-link text-white">📅 Meus Eventos Criados</a>
            </li>
            <li class="nav-item">
                <a href="/eventos/proximos" class="nav-link text-white">⏳ Próximos Eventos</a>
            </li>
            <li class="nav-item">
                <a href="/eventos/eventosinscritos" class="nav-link text-white">🔜 Eventos Inscritos</a>
            </li>
        </ul>
    </nav>


<div class="container mt-4">
    <h1 class="text-center">Eventos Inscritos</h1>
    <hr>

    {{#if eventosInscritos.length}}
        <div class="list-group">
            {{#each eventosInscritos}}
                <div class="list-group-item mb-2">
                    <h5>{{this.titulo}}</h5>
                    <p><strong>Data:</strong> {{formatarData this.data}}</p>
                    <p><strong>Local:</strong> {{this.local}}</p>
                    <p><strong>Criado por:</strong> {{this.criador.nomeDeUsuario}}</p>

                    <div class="d-flex gap-2">
                        <form action="/eventos/desinscrever/{{this._id}}" method="POST">
                            <button class="btn btn-warning btn-sm unsubscribe-event">Desinscrever</button>
                        </form>
                            <a href="/eventos/vermais/{{this._id}}" class="btn btn-primary btn-sm">Ver Mais</a>
                    </div>
                </div>
            {{/each}}
        </div>
    {{else}}
        <p class="text-center">Você não está inscrito em nenhum evento.</p>
    {{/if}}
</div>